{{#each posts as |post|}}
<div class="row">
    <div class="col-md-12">
    <h2>{{post.title}}</h2>
    <p>{{post.date_created}}</p>
    <h3>{{user.name}} said..</h3>
    <p>{{post.post_body}}</p>
    </div>
</div>
<hr>
{{/each}}